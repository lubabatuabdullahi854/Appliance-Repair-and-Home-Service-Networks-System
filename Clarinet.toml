[project]
name = 'Appliance-Repair-and-Home-Service-Networks-System'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.technician-registry]
path = 'contracts/technician-registry.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
